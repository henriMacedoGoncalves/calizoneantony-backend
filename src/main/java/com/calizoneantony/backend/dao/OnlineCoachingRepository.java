package com.calizoneantony.backend.dao;

import com.calizoneantony.backend.entity.OnlineCoaching;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OnlineCoachingRepository extends JpaRepository<OnlineCoaching, Long> {
}
